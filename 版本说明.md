# 版本说明 — PulseWin（Win11 AI 额度浮窗）

> 当前版本：**v1.13.0**（2026-09-26 接入 DSH 用量来源与活动指示）
> 首版完成日期：2026-09-08
> 本文件从 v1.13.0 起逐版维护；v1.12.2 及更早的完整改动见 `git log` 与 `README.md`。

## 功能

- **悬浮 rail**：屏幕边缘一条黑色浮窗，每个订阅一个复合同心环（内细圈 = 5 小时用量、外粗圈 = 月总额度），
  ≤80% 绿、>80% 红；hover 弹明细卡；贴边收起、全屏自动隐藏、可拖动与排序。
- **额度源**：Command Code GOAT（credits 口径）、OpenCode Go、DeepSeek（余额型），60 秒自动同步，
  凭据 DPAPI 加密存本地，不依赖第三方服务。
- **用量窗口**（托盘右键 → 用量）：订阅额度条 + 总览 + 套餐三层。数据来自本机三个来源：
  ZCode（`~\.zcode\cli\db\db.sqlite`）、OpenCode（`opencode.db`）、
  **DSH（`~\.dsh\sessions\` 的 zstd 会话事件流）**。
- **套餐页**：按套餐（渠道）聚合一页一页看——KPI、Token 构成、今日逐小时、时段分布、模型分布、逐日趋势。
- **活动环心呼吸**：ZCode 或 DSH 正在跑一轮时，GOAT 环的图标轻轻呼吸。
- **离屏出图诊断**：`--spend` / `--models` / `--dsh` / `--activity` / `--card-shot` / `--ui-shot` / `--rail-shot` /
  `--check-migration` / `--update-prices`（都不开界面、只出结果文件）。

## 版本历史

### v1.13.0（2026-09-26）— 接入 DSH（DeepSeek Harness）用量与活动指示

**背景**：开始日常使用 DSH 之后，它的消耗在本机用量里是**看不见的**——用量窗只读 ZCode 与 OpenCode 的库，
于是 DSH 花掉的 token 全被算进"官方账期 vs 本机"的差额里（像"其他设备"一样）。

- **新增用量来源 `DshUsageStore`**：直接读 DSH 的会话事件流
  `~\.dsh\sessions\<工作区>\<会话>\session.v4.jsonl.zstd`，按 `assistant/message` 事件逐步取
  `usage`（inputTokens / outputTokens / cacheReadTokens / totalTokens）与 `message.source`（provider / model），
  从 `session` 行取会话号与工作目录、`session/title` 取标题，从 `step/start` 与首个流式 chunk 算耗时与首字延迟。
  实测：23 个会话、首次全量解析约 0.7 秒。
- **口径（与 ZCode 相反，勿照抄）**：DSH 的 `inputTokens` 是**未命中缓存的输入**
  （会话投影里叫 `uncachedInputTokens`），`cacheWriteTokens` 恒为 0，
  `totalTokens ≡ input + output + cacheRead`（1166 步实测恒等、未分类恒为 0）。四类直接取用，不做减法。
- **容错解压 + 文件级缓存**：活动会话随时可能只写到半个 zstd 帧 → 读到哪算哪、下一轮长度变了再重读补齐；
  历史会话内容不变 → 按（长度 + 修改时间）命中即复用。缓存跨账本存活（有硬上限并 LRU 淘汰），
  否则一个季度后每次打开用量窗都要冷启动解压几百个会话。
- **渠道归属**：`ChannelRegistry` 补 `commandcode-goat` / `commandcode` / `goat` → Command Code GOAT、
  `deepseek` → DeepSeek 的内置规则。DSH 记的是**路由名**（不是能查到的 provider id），
  不补规则会多出一行"渠道 commandco"，而它花的正是 GOAT 那份额度。
- **DSH 活动指示 `DshActivity`**：读会话投影 `~\.dsh\storages\session_projcache\sessions\*.json` 的
  `turnBoundary.openTurnStartSeq`（有没有未闭合的回合）与 `sessionStats.openStep.startTime`，
  带 5 分钟门限——异常退出会留下永不闭合的回合，没有门限会一直显示在跑。
  ZCode 那套判定不动；两者任一在跑，GOAT 环心就呼吸。
- **新诊断 `--dsh`**：解析结果、冷/热耗时、缓存命中，以及**与 DSH 自己的会话投影逐会话对账**
  （16 个会话里 14 个完全一致，2 个不一致的都是活动会话且方向是"本机 > 投影"，即投影落后而非漏读）。
  `--activity` 同时输出 DSH 的判定。
- **依赖**：新增 `ZstdSharp.Port`（纯托管，无原生 dll，不影响单文件发布）。
- **顺手修正一处"把不知道说成没有"**：`ReasoningShare` 原先只要"有输出"就返回 0%，
  于是**没记推理 token 的来源**（DSH 的 `outputTokens` 含推理却不单列，ZCode 的列可能缺）
  会显示成"推理 0%（没有思考）"。现改为**只有真的报了推理 token 才算占比**，否则显示 `—`。
- **验收**：`--spend` 显示来源 `[ZCode, OpenCode, DSH]`；渠道页第一页副标题为"DSH + ZCode 渠道"，
  即两者已并进同一个 GOAT 套餐页；验收时 DSH 当日已记到约 1.5 亿 token / 1100+ 次调用。
- 仓库根目录的 `PulseWin.exe` 换成 **v1.13.0 框架依赖单文件**（旧版备份 `PulseWin.exe.v1.12.2.old`）；
  **替换后需重启程序才生效**。

### v1.7.1 ~ v1.12.2（2026-09-25 ~ 09-26）— 摘要（详见 git log 与记忆库）

- v1.12.x：统计窗与设置里的套餐页**合并为独立"用量"窗口**（额度条 + 总览 + 套餐三层），
  环比、下钻、CSV/JSON 导出、自定义区间、GOAT 峰谷拆分、托盘阈值告警、账号计量对照；
  修"一直读取中"笔误、托盘回品牌图标。
- v1.11.x：**「圆环顺序」改为可重排列表**（N! → N）、**数据源改为注册表驱动**（新增源只加一条记录）；
  内圈按需显示（5 小时窗口有用量才画）；诊断模式不再弹模态框。
- v1.10.x：修统计页"最忙小时永远是 12:00"的真 bug（小时分布只认真小时行）、卡片紧凑化、
  色块按钮文字被裁；新增 `--card-shot` / `--ui-shot`。
- v1.9.x：设置新增「套餐用量」页（按套餐分桶，**按套餐名而非 provider id**，避免同一套餐裂成多行）；
  "当前区间没数据"的套餐从配置预建桶列出，不再静默消失。
- v1.8.x：模型页 + `SpendLedgerCache` 账本共享缓存；修卡片行文字被右缘裁掉。
- v1.7.x：修"调一次 SetWindowCompositionAttribute 永久打坏分层窗口 alpha"（圆弧变黑矩形）、
  TextBox padding 裁掉句点；内存画像与两档节拍（私有 340→172 MB、idle CPU 9.6%→0%）；
  **整体删除液态玻璃**；修"贴边小条/全屏隐藏"两个开关从不生效、
  以及 P/Invoke 把 `GetWindowRect` 声明错导致的"Entry point not found"卡死。

## 已知限制

- 框架依赖单文件需要机器上有 .NET 8 桌面运行时；给别的机器用自包含版。
- 用量金额是**按公开牌价折算的估算**，不是账单；查不到价的模型只计 token、金额显示 `—`。
- DSH 的推理 token 不单列（`outputTokens` 已含推理），所以"推理占比"这一项对 DSH 记录恒为空。
